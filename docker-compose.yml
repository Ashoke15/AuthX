services:
  mailpit:
    image: axllent/mailpit
    container_name: auth-service-mailpit
    ports:
      - "1025:1025"   # SMTP — your app sends here
      - "8025:8025"   # Web UI — view caught emails at http://localhost:8025
    restart: unless-stopped